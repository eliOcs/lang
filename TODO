- Lexer
- Parser
- Interpreter

Ideas

functions
    - named parameters (map)
    - list parameters
    - default values
    - optional parameters

packages
    - Go style
    - public/private - with the export keyword

scope
    - GO style

syntax
    - integrate event handling system (on/off/trigger)
    
    disrupt conventional syntax
        - allow white space on identifiers
        - use symbols for reserved words
        
        add two numbers (a, b) -> result {
            a + b
        }
        
        variable -> "lemon"
        
        if (variable = "lemon") {
            print(true)
        }
        
        if (variable = "lemon", {
        
        }, {
        
        })
        
        print(variable)

macros

Documentation
    - Python, Clojure, Elixir type
